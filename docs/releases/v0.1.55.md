# Entire Replay Lab v0.1.55

Non-blank file evidence entries.

## What Changed

- Replay file-list arrays now reject empty or whitespace-only string entries in
  the schema contract.
- Applied non-blank string checks to `changed_files`, `files_touched`, and
  metric file buckets.
- Added validator negative checks that mutate file-list arrays to blank values
  and prove those payloads are rejected.
- Updated acceptance, architecture, testing, command, and JSON schema docs.

## Why It Matters

File lists are the core evidence behind replay comparison. A blank string can
technically pass a plain string item rule, but it cannot identify a changed,
touched, missing, extra, or risky file. This release keeps file evidence arrays
both deduplicated and meaningful at the schema-validation layer used by docs,
smoke, CI, and release checks.

## Verified

- `python3 ./scripts/validate-examples.py`
- `python3 ./scripts/validate-release-docs.py`
- `python3 ./scripts/validate-script-hygiene.py`
- `./scripts/verify-reproducibility.sh`
- `./scripts/verify-patch-manifest.sh`
- `./scripts/check-refresh-patch.sh`
- `./scripts/verify-repo.sh`
- `./scripts/release-check.sh`
- `./scripts/smoke.sh /Users/suhaan/Documents/Ultron`
- GitHub Actions CI

## Known Limitations

- This remains a prototype patch repo, not an upstream Entire CLI release.
- Live replay still requires each selected coding agent to be installed and
  authenticated locally.
- Pi is included in all-agent eval output, but skipped until a safe
  non-interactive launch contract is available.
